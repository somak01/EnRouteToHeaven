#include<stdio.h>


int main() {
	//int size_one;
	//int size_two;
	//int size_three;
	int size_one = 600,size_two = 200, size_three = 300;
	printf("The average of the 3 values is: %.2f", (size_one + size_two + size_three) / 3.0);
	return 0;
}
